# GENERATED by `dfx job add` from specs/gbs_auth/platform_b_orders.md — regenerate with --force.
# Included automatically by the project-level databricks.yml (resources/**/*.job.yml).
resources:
  jobs:
    gbs_auth_platform_b_orders:
      name: "dfx-${var.environment}-gbs-auth-platform-b-orders"
      tags:
        framework: df-dbx-rt-ingestion-workflow
        domain: gbs_auth
        platform: platform_b
      continuous:
        pause_status: UNPAUSED
      tasks:
        - task_key: ingest
          spark_python_task:
            python_file: ./platform_b_databricks.py
            parameters:
              - "--env"
              - "${var.environment}"
              - "--config"
              - "${workspace.file_path}/conf/jobs/gbs_auth/gbs_auth_platform_b_orders.yaml"
              - "--conf-dir"
              - "${workspace.file_path}/conf"
          libraries:
            - whl: ../../dist/*.whl
          new_cluster:
            spark_version: ${var.spark_version}
            node_type_id: ${var.node_type_id}
            num_workers: 2
            data_security_mode: SINGLE_USER
            spark_conf:
              spark.databricks.delta.optimizeWrite.enabled: "true"
