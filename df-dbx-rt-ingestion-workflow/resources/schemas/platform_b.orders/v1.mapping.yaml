# Schema mapping: platform_b.orders v1
# Source: topic gbs-auth.platform-b.orders.v1 -> target main.gbs_auth.platform_b_orders
# GENERATED by `dfx job add` from specs/gbs_auth/platform_b_orders.md — regenerate with --force.
columns:
  - target: order_id
    source: ord_id
  - target: customer_id
    source: cust_id
  - target: amount_usd
    expr: "amt * fx_rate"
    type: decimal(18,2)
    description: "Amount converted to USD"
  - target: status
    source: status_cd
    default: "UNKNOWN"
  - target: order_ts
