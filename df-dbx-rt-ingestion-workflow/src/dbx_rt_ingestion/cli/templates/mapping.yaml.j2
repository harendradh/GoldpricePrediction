# Schema mapping: {{ topic.schema_subject }} v1
# Source: topic {{ topic.name }} -> target {{ sink.full_table }}
# GENERATED by `dfx job add` from {{ spec_source }} — regenerate with --force.
columns:
{% for m in topic.mapping %}
  - target: {{ m.target }}
{% if m.expr %}
    expr: {{ m.expr | tojson }}
{% elif m.source and m.source != m.target %}
    source: {{ m.source }}
{% endif %}
{% if m.type %}
    type: {{ m.type }}
{% endif %}
{% if m.default is not none %}
    default: {{ m.default | tojson }}
{% endif %}
{% if m.description %}
    description: {{ m.description | tojson }}
{% endif %}
{% endfor %}
