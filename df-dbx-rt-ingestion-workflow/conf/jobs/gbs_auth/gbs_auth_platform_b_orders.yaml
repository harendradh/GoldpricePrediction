# GENERATED by `dfx job add` from specs/gbs_auth/platform_b_orders.md
# Do not edit by hand — update the functional spec and re-run:
#   dfx job add --spec specs/gbs_auth/platform_b_orders.md --force
name: gbs-auth-platform-b-orders
domain: gbs_auth
platform: platform_b
version: 1.0.0
description: Platform B order events from AWS MSK into the auth lakehouse.

source:
  type: kafka_msk
  cluster: msk-primary
  options:
    startingOffsets: earliest
    maxOffsetsPerTrigger: '500000'
  topics:
    - name: gbs-auth.platform-b.orders.v1
      schema:
        subject: platform_b.orders
        version: v1
      parser:
        type: json
      mapping_file: resources/schemas/platform_b.orders/v1.mapping.yaml
      quality:
        - name: order_id_present
          type: not_null
          column: ord_id
          action: dlq
        - name: valid_status
          type: allowed_values
          column: status_cd
          options:
            values:
            - NEW
            - FILLED
            - CANCELLED
          action: warn

sink:
  type: delta
  table: main.gbs_auth.platform_b_orders
  mode: merge
  merge_keys:
    - order_id
  trigger:
    processingTime: 30 seconds

schema_management:
  repository: file
  options:
    base_path: resources/schemas

checkpoint_root: /Volumes/main/gbs_auth/checkpoints/gbs-auth-platform-b-orders

reliability:
  dlq:
    enabled: true
    table: main.gbs_auth.platform_b_orders_dlq
  retry:
    max_attempts: 3
    initial_backoff_seconds: 5
  graceful_shutdown_marker: /Volumes/main/gbs_auth/control/gbs-auth-platform-b-orders.stop

observability:
  audit_table: main.gbs_auth.dfx_audit
  publishers:
    - type: log

feature_flags: {}

tags:
  cost_center: gbs-auth-migration
  support_team: gbs-streaming-ops
